# 🚀 Інструкція по розгортанню Coffee Shop

## 📋 Зміст
1. [Підготовка проекту](#підготовка-проекту)
2. [Розгортання Backend на Render](#розгортання-backend-на-render)
3. [Розгортання Frontend на Vercel](#розгортання-frontend-на-vercel)
4. [Налаштування змінних середовища](#налаштування-змінних-середовища)
5. [Тестування](#тестування)

---

## 🔧 Підготовка проекту

### 1. Переконайтеся що проект залитий на GitHub
```bash
git status
git add .
git commit -m "Ready for deployment"
git push origin main
```

### 2. Підготуйте необхідні облікові записи:
- ✅ GitHub аккаунт (вже є)
- ✅ [Render](https://render.com) - для backend (безкоштовно)
- ✅ [Vercel](https://vercel.com) - для frontend (безкоштовно)
- ✅ [Stripe](https://stripe.com) - для оплати (тестовий режим)
- ✅ Gmail аккаунт - для відправки листів

### 3. Список ключів які вам знадобляться:

#### Для Backend (Render):
- `JWT_SECRET` - згенеруйте випадковий рядок (мін. 32 символи)
- `STRIPE_SECRET_KEY` - з Stripe Dashboard (починається з `sk_test_`)
- `EMAIL_USER` - ваш Gmail адрес
- `EMAIL_PASS` - App Password з Gmail (16 символів без пробілів)

#### Для Frontend (Vercel):
- `VITE_API_URL` - URL вашого backend з Render (отримаєте після деплою)
- `VITE_STRIPE_PUBLIC_KEY` - з Stripe Dashboard (починається з `pk_test_`)

---

## 🖥️ Розгортання Backend на Render

### Крок 1: Створення Web Service

1. Перейдіть на [Render Dashboard](https://dashboard.render.com/)
2. Натисніть **"New +"** → **"Web Service"**
3. Підключіть ваш GitHub репозиторій: `Kolaaa25/coffee-shop`

### Крок 2: Налаштування сервісу

Використайте наступні налаштування:

```
Name: coffee-shop-backend
Region: Frankfurt (EU Central) або найближчий
Branch: main
Root Directory: backend
Runtime: Node
Build Command: npm install
Start Command: npm start
```

### Крок 3: Виберіть план
- Оберіть **Free** план (0$/місяць)
- ⚠️ Увага: безкоштовний план засинає після 15 хвилин неактивності

### Крок 4: Налаштуйте змінні середовища (Environment Variables)

Додайте наступні змінні:

```bash
NODE_ENV=production
PORT=5001
FRONTEND_URL=https://your-app.vercel.app  # Замініть після створення Vercel

# JWT Secret - згенеруйте свій унікальний ключ (мінімум 32 символи)
# Приклад генерації: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
JWT_SECRET=your_super_secret_jwt_key_min_32_characters_long_12345

# Stripe Keys - отримайте ваші власні ключі на https://dashboard.stripe.com/test/apikeys
STRIPE_SECRET_KEY=sk_test_51QMlIH...  # Замініть на ваш Secret key

# Email Configuration - використайте ваш Gmail
EMAIL_HOST=smtp.gmail.com
EMAIL_PORT=587
EMAIL_USER=your-email@gmail.com  # Ваш Gmail адрес
EMAIL_PASS=xxxx xxxx xxxx xxxx  # 16-символьний App Password без пробілів
EMAIL_FROM=your-email@gmail.com  # Той самий Gmail адрес
```

### Крок 5: Створення App Password для Gmail

1. Перейдіть на https://myaccount.google.com/security
2. Увімкніть **2-Step Verification** (двофакторну аутентифікацію) якщо не увімкнена
3. Поверніться на https://myaccount.google.com/security
4. Прокрутіть вниз до **App passwords** (Паролі додатків)
5. Натисніть **App passwords**
6. У полі "App name" введіть: `Coffee Shop Backend`
7. Натисніть **Create**
8. Скопіюйте 16-символьний пароль (наприклад: `abcd efgh ijkl mnop`)
9. Вставте його в `EMAIL_PASS` **БЕЗ пробілів**: `abcdefghijklmnop`

⚠️ **ВАЖЛИВО**: 
- Це НЕ ваш звичайний пароль від Gmail
- Пароль показується тільки один раз, збережіть його
- Без 2FA ви не зможете створити App Password

### Крок 6: Деплой
- Натисніть **"Create Web Service"**
- Почекайте 3-5 хвилин поки Render збере і запустить проект
- Після успішного деплою ви отримаєте URL типу: `https://coffee-shop-backend-xxxx.onrender.com`

### Крок 7: Перевірка
Відкрийте в браузері:
```
https://your-backend-url.onrender.com/api/health
```
Повинен повернутись відповідь: `{"status":"ok"}`

---

## 🌐 Розгортання Frontend на Vercel

### Крок 1: Імпорт проекту

1. Перейдіть на [Vercel Dashboard](https://vercel.com/dashboard)
2. Натисніть **"Add New..."** → **"Project"**
3. Знайдіть ваш GitHub репозиторій `coffee-shop`
4. Натисніть **"Import"**

### Крок 2: Налаштування проекту

```
Project Name: coffee-shop
Framework Preset: Vite
Root Directory: frontend
Build Command: npm run build
Output Directory: dist
Install Command: npm install
```

### Крок 3: Налаштування змінних середовища

Додайте змінні в **Environment Variables**:

```bash
# Backend API URL - замініть після деплою на Render
VITE_API_URL=https://your-backend-url.onrender.com

# Stripe Publishable Key - отримайте на https://dashboard.stripe.com/test/apikeys
# Це ПУБЛІЧНИЙ ключ, він починається з pk_test_
VITE_STRIPE_PUBLIC_KEY=pk_test_51QMlIH...  # Замініть на ваш Publishable key
```

⚠️ **Важливо:** Замініть `your-backend-url` на реальний URL з Render!

### Крок 4: Деплой
- Натисніть **"Deploy"**
- Почекайте 2-3 хвилини
- Після успішного деплою отримаєте URL: `https://coffee-shop-xxxx.vercel.app`

### Крок 5: Оновіть Backend
Поверніться в Render і оновіть змінну:
```bash
FRONTEND_URL=https://coffee-shop-xxxx.vercel.app  # Ваш реальний Vercel URL
```

Після зміни натисніть **"Manual Deploy"** → **"Deploy latest commit"**

---

## 🔐 Налаштування Stripe

### 1. Отримайте ваші тестові ключі

1. Перейдіть на [Stripe Dashboard](https://dashboard.stripe.com/test/apikeys)
2. Переключіться на **Test mode** (тоглер вгорі справа)
3. Знайдіть розділ **API keys**
4. Скопіюйте ДВА ключі:
   - **Publishable key** (починається з `pk_test_`) → для Vercel `VITE_STRIPE_PUBLIC_KEY`
   - **Secret key** (натисніть "Reveal", починається з `sk_test_`) → для Render `STRIPE_SECRET_KEY`

⚠️ **ВАЖЛИВО**: 
- Secret key НІКОЛИ не додавайте у frontend код
- Publishable key можна використовувати публічно
- Не плутайте ці два ключі місцями!

### 2. Налаштуйте Webhook (опціонально для production)

1. В Stripe Dashboard → **Developers** → **Webhooks**
2. Додайте endpoint: `https://your-backend-url.onrender.com/api/payment/webhook`
3. Виберіть події: `checkout.session.completed`, `payment_intent.succeeded`
4. Скопіюйте **Signing secret** → `STRIPE_WEBHOOK_SECRET`

### 3. Тестові картки Stripe

Використовуйте для тестування:
```
Успішна оплата: 4242 4242 4242 4242
CVC: Будь-які 3 цифри
Дата: Будь-яка майбутня дата
```

---

## 🧪 Тестування

### Перевірка Backend:

```bash
# Health check
curl https://your-backend-url.onrender.com/api/health

# Отримати меню
curl https://your-backend-url.onrender.com/api/menu
```

### Перевірка Frontend:

1. Відкрийте ваш Vercel URL в браузері
2. Перевірте:
   - ✅ Головна сторінка завантажується
   - ✅ Меню відображається
   - ✅ Можна додати товар в кошик
   - ✅ Можна перейти до оплати
   - ✅ Форма контактів працює

---

## 🔄 Автоматичний деплой

Обидва сервіси (Render і Vercel) автоматично деплоять при push в GitHub:

```bash
# Внесіть зміни
git add .
git commit -m "Update: ваші зміни"
git push origin main

# Render і Vercel автоматично задеплоять нову версію
```

---

## 🐛 Типові проблеми та рішення

### Backend не запускається на Render
- ✅ Перевірте логи в Render Dashboard
- ✅ Переконайтеся що всі змінні середовища встановлені
- ✅ Перевірте що `package.json` містить `"start": "node server.js"`

### Frontend не бачить Backend
- ✅ Перевірте `VITE_API_URL` в Vercel
- ✅ Переконайтеся що Backend працює
- ✅ Перевірте CORS налаштування в `backend/server.js`
- ✅ Оновіть `FRONTEND_URL` в Render

### Email не відправляються
- ✅ Перевірте що використовуєте App Password, а не звичайний пароль
- ✅ Переконайтеся що 2FA увімкнена в Gmail
- ✅ Перевірте всі EMAIL змінні в Render

### Stripe оплата не працює
- ✅ Переконайтеся що використовуєте Test mode ключі
- ✅ Перевірте що `VITE_STRIPE_PUBLIC_KEY` встановлена в Vercel
- ✅ Використовуйте тестову картку: 4242 4242 4242 4242

### Render засинає (Free tier)
- ℹ️ Це нормально для безкоштовного плану
- ℹ️ Перший запит після сну займе 30-50 секунд
- 💡 Рішення: апгрейд до платного плану або використайте інший сервіс

---

## 📝 Чеклист перед деплоєм

Backend (Render):
- [ ] Репозиторій залитий на GitHub
- [ ] Створений Stripe аккаунт
- [ ] Створений Gmail App Password
- [ ] Налаштовані всі змінні середовища
- [ ] Backend успішно задеплоєний

Frontend (Vercel):
- [ ] Backend URL додано в `VITE_API_URL`
- [ ] Stripe публічний ключ додано
- [ ] Frontend успішно задеплоєний
- [ ] Backend оновлений з Frontend URL

Тестування:
- [ ] Меню завантажується
- [ ] Кошик працює
- [ ] Оплата працює
- [ ] Форма контактів працює
- [ ] Email доставляються

---

## 🎉 Готово!

Ваш Coffee Shop тепер онлайн!

- 🌐 **Frontend**: https://your-app.vercel.app
- 🖥️ **Backend**: https://your-backend.onrender.com
- 💳 **Stripe Dashboard**: https://dashboard.stripe.com/test

---

## 📞 Підтримка

Якщо виникли проблеми:
1. Перевірте логи в Render Dashboard
2. Перевірте логи в Vercel Deployments
3. Перевірте браузерну консоль (F12)
4. Створіть issue на GitHub

---

**Успіхів з деплоєм! ☕✨**
